# framework/venv

VirtualEnv should be set up here. The .gitignore discards everything except the
requirements.txt file. To set up, run:
- cd framework/venv/
- python3 -m venv .
- python3 -m pip install -r requirements.txt
